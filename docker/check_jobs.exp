#!/usr/bin/expect -f
set timeout 30
log_user 1
spawn nc localhost 2323
expect "Connected"
sleep 2
send "\r"
expect "User:"
send "\[1,2\]\r"
expect -re "\[Pp\]assword"
send "Digital1977\r"
expect {
    "Job number to attach to?" { send "\r" }
    -re {\$} {}
}
expect -re {\$}
send "SYSTAT\r"
expect -re {\$}
send "BYE\r"
expect eof
