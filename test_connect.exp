#!/usr/bin/expect -f
set timeout 30
spawn telnet localhost 2323
expect "Connected to"
sleep 2
send "\r"
sleep 2
expect {
    "User:" { send "\[1,2\]\r" }
    "$" { puts "Got prompt" }
    "disabled" { puts "Logins disabled" }
    timeout { puts "Timeout" }
}
sleep 5
interact
