	PROGRAM	PRIME

	INTEGER	LIMIT, J, NUM

	TYPE	10
10	FORMAT	(///, ' Primes up to : ',$)
	ACCEPT	20, LIMIT
20	FORMAT	(I10)
	DO	1000, NUM=3, LIMIT, 2
	DO	100, J=3, INT(SQRT(FLOAT(NUM))), 2
	IF	(MOD(NUM,J) .EQ. 0) GOTO 1000
100	CONTINUE
	TYPE	20,NUM
1000	CONTINUE
	CALL	EXIT

	END
